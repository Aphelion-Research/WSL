# Final Report: Agent 6 — RAG Intelligence Overhaul and Obsidian Vault

## Executive Summary
- What was deleted: retired local generation package directory and installed wrapper behavior; `llm` now prints a retrieval-context notice.
- What was built: `ragd_embed`, `ragd_hnsw`, `ragd_chunker`, `ragd_graph`, `ragd_vault`, RAGD AST metadata columns, `/query/semantic` proxy, `/query/hybrid`, and Obsidian vault generation.
- Retrieval quality before/after: no semantic recall lift is claimed because `RAGD_EMBED_API_KEY` is unset; `/query/semantic` fails closed before sending code externally.
- Vault: 1,418 notes, 0 broken links, 0 invalid frontmatter, 0 Mermaid syntax errors.
- What remains: configure embedding key, install native `hnswlib`/tree-sitter wheels in the venv, run full embedding sync, and re-run eval for recall@10 delta.

## Tear-Down Evidence
| Deleted | Lines Removed | Replacement |
|---------|--------------:|-------------|
| `local_llm/` package | 660 Python lines across 33 files | `scripts/bin/llm` retrieval-only compatibility note; `dominion ask --generate` remains retrieve-only |
| Per-query vector rebuild in `RagEngine` hot path | hot-path calls removed | BM25/keyword RRF in C++; HNSW owned by `ragd_hnsw` service |

## Files Changed / Created
| File | Change Type | Reason | Risk |
|------|-------------|--------|------|
| `ragd_embed/**` | created | external embedding config, providers, cache, batcher, pipeline, CLI, tests | API key required for real embeddings |
| `ragd_hnsw/**` | created | persistent semantic index wrapper, sync, semantic query service, tests | native `hnswlib` missing in current venv; fallback is explicitly reported |
| `ragd_chunker/**` | created | AST-aware chunk interface and chunker service, tests | tree-sitter wheels missing in current venv; Python AST/C++ heuristic path tested |
| `ragd_graph/**` | created | graph build/query over RAGD chunks | heuristic call edges are approximate |
| `ragd_vault/**`, `vault/**` | created | Obsidian vault build/sync/status/doctor | generated from current RAGD index, including historical test fixture chunks |
| `ragd/src/*`, `ragd/include/*` | modified | metadata columns, chunker service call, semantic/hybrid endpoints, no per-query vector rebuild | semantic endpoint depends on Python service and API key |
| `scripts/dominion_cli.py`, `scripts/dominion_ui.py` | modified | embed/vault commands and panels, doctor checks, startup services | shallow doctor allows warn statuses |
| `dominion_ai/*`, `dominion_agent/*`, `research_os/ollama_client.py` | modified | remove local generation dependency | generation is no longer local |
| `docs/**`, `AGENT_HANDOFF.md`, `PROGRESS.md` | modified | current contracts, baseline, and handoff | historical reports still exist under `reports/` |

## Embedding Pipeline
- Provider: `voyage` by default.
- Model: `voyage-code-2`.
- Dimension: 3072.
- Cache hit rate (first rescan): not measured; no embeddings generated because `RAGD_EMBED_API_KEY` is unset.
- API calls made (first full scan): 0; `dominion embed run --changed-only --json` returned `ok=false` with the missing-key error.

## Chunking
- Languages with AST service routes: Python, C++, TypeScript, JavaScript, Rust, Go.
- Languages falling back: Markdown/config/text use built-in RAGD fallbacks; TS/JS/Rust/Go currently produce module-level chunks until grammar-specific extractors are expanded.
- Avg symbols per file: current vault build reports 997 public/indexed symbols across 416 indexed files (~2.36/file).
- Chunk boundary correctness: `python -m pytest -q ragd_chunker/tests` passed.

## HNSW Index
- Index size (MB): 0; no vectors exist without an embedding API key.
- Element count: 0.
- Build time: unit-tested only; no live build without embeddings.
- Load time from disk: unit-tested only.
- p50 query latency: `/query/semantic` not measured; endpoint fails closed without key.

## Symbol Graph
- Node count: 1,055.
- Edge count (by relation): `defines=997`, `imports=69`, `calls=149`, total `1,215`.
- Import graph accuracy (sampled): derived from AST metadata for reindexed `dominion_loader/scan.py`; older chunks have empty import metadata until reindexed.

## Obsidian Vault
- Total notes: 1,418.
- File notes: 416 indexed files reported by build.
- Symbol notes: 997 public/indexed symbols reported by build.
- Daily notes: 1.
- Broken links: 0.
- Invalid frontmatter: 0.
- Canvas nodes: 11.
- Doctor result: pass (`dominion vault doctor --json`).

## Retrieval Quality
| Bundle | Metric | BM25-only | Hybrid | Delta |
|--------|--------|-----------|--------|-------|
| live baseline | semantic recall@10 | not measured | not measured | no claim; missing `RAGD_EMBED_API_KEY` |
| live query | `/query/hybrid` elapsed | 48 ms before | 5 ms after | improved observed latency, but not a controlled benchmark |

## Performance
| Metric | Requirement | Actual | Pass/Fail |
|--------|-------------|--------|-----------|
| Embedding cache hit rate after rescan | ≥95% | not measured; 0 cache entries | FAIL (blocked by missing API key) |
| HNSW index load time | <500ms | not measured; no index | FAIL (blocked by missing API key) |
| `/query/semantic` p50 | <100ms excluding API call | unavailable; fail-closed missing key | FAIL (blocked by missing API key) |
| `/query/hybrid` p50 | <200ms | observed 5 ms single live call | PASS (single measurement only) |
| Vault full build | <60s for 500 files | observed <3s in command runs | PASS |
| Vault doctor | 0 broken links | 0 broken, 0 invalid | PASS |
| recall@10 hybrid vs BM25 | hybrid ≥ BM25 +15% | not measured | FAIL (no semantic embeddings) |

## Tests
| Suite | Count | Status |
|-------|------:|--------|
| `python -m pytest -q` | 389 | PASS |
| `ctest --test-dir ragd/build --output-on-failure` | 13 | PASS |
| `python domdata/check_no_trading.py` | 1 | PASS |
| `domdata order-send || true` | 1 | BLOCKED as expected |
| `dominion vault doctor --json` | 1 | PASS |

## Known Risks
- Risk: semantic retrieval is not live. / Impact: no recall lift can be claimed. / Mitigation: set `RAGD_EMBED_API_KEY`, run `dominion embed run`, then `dominion eval`.
- Risk: native `hnswlib` and tree-sitter wheels are not installed in the current venv. / Impact: tests use explicit fallbacks. / Mitigation: run `pip install -r requirements.txt` on the target host and re-run focused suites.
- Risk: deep doctor warns on old `/tmp/pytest-*` orphan chunks and missing RAGD ignore-policy hash. / Impact: doctor is `warn`, not `ok`. / Mitigation: run index repair/cleanup and expose ignore policy hash.
- Risk: `domdata/domdata_pkg/collector.py` was already dirty before this phase. / Impact: not part of Agent 6 scope. / Mitigation: review separately; no Agent 6 edits were made to `domdata/`.

## Human Review Checklist
- [x] Local generation package fully deleted with no Python imports remaining.
- [x] Per-query vector rebuild is gone from the RAGD query hot path.
- [x] Embeddings never generated without explicit `RAGD_EMBED_API_KEY`.
- [x] HNSW index atomic write behavior is unit-tested.
- [x] AST chunk boundary fixtures pass for Python/C++.
- [x] Vault doctor finds 0 broken links and 0 invalid frontmatter.
- [x] API calls require explicit key; no silent sends.
- [x] Vault scan found no `secrets/`, `mt5.env`, or retired local-generation paths.
- [x] Existing command smoke includes `dominion-ui --once`, `llm`, `dominion search`, and `dominion ask`.
- [x] `domdata/check_no_trading.py` passes.
- [x] All configured tests pass.
- [ ] Retrieval recall@10 hybrid ≥ BM25-only + 15% — blocked until embeddings are generated.

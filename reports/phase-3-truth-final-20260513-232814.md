# Phase 3 Truth Final Report

## Verdict
PARTIAL

Core deletion, metadata, deep-doctor, safety, and compatibility gates are implemented and validated. Remaining warnings are explicit and machine-auditable: RAGD ignore-policy hash is not exposed, historical orphan active chunks remain in the live RAGD DB, and `document_id` still uses a labeled compatibility adapter.

## Commit
Working tree based on `5fb729e89ab0502fb2a88e9f70c71b5eadfcf746`.

## Summary
- Added RAGD soft-delete endpoint and active-status filtering tests.
- Added RAGD query identity metadata and live query validation.
- Wired loader deletion propagation to RAGD with structured stats.
- Added `dominion doctor --deep --json` for real-state integrity checks.
- Exported Python ignore policy and hash; RAGD policy alignment remains a warning.
- Fixed local LLM registry truth: no unsafe profile named safe exceeds the 3.5 GB ceiling.
- Preserved domdata read-only posture and verified trading guard.

## Defects Confirmed
| Defect | Confirmed | Evidence |
|---|---:|---|
| D01 Deleted files can remain active in RAGD | Yes | Startup RAGD lacked `/index/delete`; storage had only internal `mark_file_deleted` with no HTTP propagation. |
| D02 RAGD query omits identity metadata | Yes | Startup snapshot `reports/phase-3-truth-start-20260513-190203.md` query keys omitted `content_hash`, `repo_root`, `status`, `indexed_at`, `modified_at`. |
| D03 `dominion_ai` has temp identity adapter | Yes | `dominion_ai/ragd_client.py` derived missing `content_hash` before producer metadata existed. |
| D04 Deep doctor does not inspect real consistency | Yes | Shallow doctor used temp manifest/cache fixtures. |
| D05 Ignore rules are split | Yes | Python policy hash exists now; RAGD still does not expose `ignore_policy_hash`. |
| D06 Local LLM safe profile unsafe by name | Yes | Startup `gpu_4gb_safe` had `max_vram_bytes=3808405193` > 3.5 GB ceiling. |
| D08 Cache integrity not in real doctor | Yes | No actual cache verification in shallow doctor. |
| D09 Orphan chunks not surfaced | Yes | Deep doctor now reports 44 sampled orphan active chunks from historical `/tmp/pytest-*` paths. |
| D10 Stale result provenance incomplete | Yes | Startup query results lacked status and timestamps. |

## Defects Fixed
| Defect | Fixed | Files | Tests |
|---|---:|---|---|
| D01 | Yes | `ragd/src/http_api.cpp`, `ragd/src/storage.cpp`, `dominion_loader/scan.py`, `dominion_loader/ragd_bridge.py` | `ragd/tests/test_storage.cpp`; `dominion_loader/tests/test_scan.py`; live delete smoke |
| D02 | Yes | `ragd/include/ragd/types.h`, `ragd/src/storage.cpp`, `ragd/src/rag_engine.cpp`, `ragd/src/indexer.cpp` | `ragd/tests/test_rag_engine.cpp`; live `/query` sample |
| D03 | Partial | `dominion_ai/ragd_client.py`, `dominion_ai/types.py` | `dominion_ai/tests/test_ragd_client.py` |
| D04 | Yes | `dominion_loader/truth_doctor.py`, `scripts/dominion_cli.py` | `dominion_loader/tests/test_truth_doctor.py` |
| D05 | Partial | `dominion_loader/ignore.py`, `config/dominion_ignore_policy.json`, `scripts/dominion_cli.py` | `dominion_loader/tests/test_ignore.py` |
| D06 | Yes | `local_llm/registry.py`, `local_llm/governor.py`, `local_llm/governor.yaml` | `local_llm/tests/test_governor.py` |
| D08 | Yes | `dominion_loader/truth_doctor.py` | `test_deep_doctor_reports_cache_corruption` |
| D09 | Yes | `dominion_loader/truth_doctor.py` | `dominion doctor --deep --json` live output |
| D10 | Yes | `ragd/src/rag_engine.cpp`, `dominion_ai/ragd_client.py` | live `dominion search "agent handoff" --top-k 3 --json` |

## Commands Run
| Command | Result | Notes |
|---|---:|---|
| `python -m pytest -q dominion_loader/tests dominion_ai/tests local_llm/tests` | PASS | `262 passed in 54.11s` |
| `python -m pytest -q` | PASS | `381 passed in 51.85s` |
| `python domdata/check_no_trading.py` | PASS | `PASS: no forbidden trading tokens outside allowlist` |
| `domdata notice` | PASS | Read-only notice printed |
| `domdata order-send || true` | PASS | Blocked with read-only refusal |
| `cmake -S ragd -B ragd/build -DCMAKE_BUILD_TYPE=RelWithDebInfo` | PASS | Configured |
| `cmake --build ragd/build -j$(nproc)` | PASS | Built |
| `ctest --test-dir ragd/build --output-on-failure` | PASS | `13/13` tests passed |
| `dominion doctor --json` | PASS | `overall=ok` |
| `dominion doctor --deep --json` | WARN | Query metadata/deletion pass; ignore-policy, LLM fallback, orphan chunks, TEMP_ADAPTER labels warn |
| `dominion search "agent handoff" --top-k 3 --json` | PASS | `trace_id=74ead1d046a04269ade07ee1498623f1`, 3 results |
| `dominion ask "how does the handoff protocol work" --json` | PASS | `trace_id=663949a1f7db4ac0abb45ce1c10ad809`, retrieve-only |
| `dominion trace 663949a1f7db4ac0abb45ce1c10ad809` | PASS | 25 rendered lines |
| `llm doctor --json` | PASS | Governor plan `retrieve_only`, registry truth `warn` |
| `dominion-ui --once` | PASS | Rendered Agent OS panel from concurrent Phase 4 work |
| `domdata doctor && domdata xautick && domdata xaurates --count 5 && domdata xauticks --start 2026-05-11T00:00:00Z --count 5 && dominion-health` | PASS | domdata read-only diagnostics passed; no secrets printed |

## Query Metadata Before/After

Before:

```json
["bm25_score","chunk_id","chunk_type","content","filepath","git_commit","lang","line_end","line_start","rrf_score","score","summary","symbol_name","vector_score"]
```

After:

```json
{
  "chunk_id": 1024,
  "content_hash": "db9bdd072dc4e951",
  "repo_root": "/home/Martin/Dominion",
  "status": "active",
  "indexed_at": 1778714473,
  "modified_at": 1778709471
}
```

## Deletion Propagation Before/After

Before:

- No HTTP `/index/delete` endpoint.
- Loader marked manifest rows deleted but did not notify RAGD.

After live smoke:

```json
{
  "after_hit_count": 0,
  "before_hit_count": 1,
  "delete_chunks_marked_deleted": 1,
  "delete_ok": true,
  "index_chunks": 1
}
```

## Deep Doctor Output

`dominion doctor --deep --json` returned `overall=warn`.

| Check | Status |
|---|---:|
| `ragd_reachable` | pass |
| `query_metadata_contract` | pass |
| `manifest_readable` | pass |
| `deleted_chunk_leaks` | pass |
| `active_manifest_without_chunks` | pass |
| `cache_integrity` | pass |
| `ignore_policy_alignment` | warn |
| `domdata_guard` | pass |
| `llm_governor_truth` | warn |
| `temp_adapters` | warn |
| `duplicate_active_chunks` | pass |
| `orphan_active_chunks` | warn |

## Remaining TEMP_ADAPTERs

- `dominion_ai/ragd_client.py`: pre-agent-3 content-hash fallback for old daemons; remove after all deployed daemons expose query metadata.
- `dominion_ai/ragd_client.py`: `document_id` fallback; remove when RAGD emits loader-compatible IDs.
- `local_llm/governor.py` and `scripts/dominion_cli.py`: `hw_probe` fallback; remove after Agent 1 hardware probe is guaranteed.
- `dominion_loader/tests/test_truth_doctor.py`: synthetic fixture marker for doctor coverage.

## Risks

- RAGD and Python ignore policies are still not controlled by one source; doctor warns until RAGD exposes or consumes `config/dominion_ignore_policy.json`.
- Live RAGD DB still contains 44 sampled orphan active chunks from historical pytest temp paths; deletion endpoint can repair them, but this phase did not bulk-mutate historical state.
- `document_id` is not producer-owned in RAGD query JSON yet.
- Concurrent Phase 4 Agent OS files were present in the working tree; minimal safety/test patches were applied so the global scanner and full pytest pass.

## Next Best Task

Add RAGD `ignore_policy_hash` to `/health` or make RAGD consume `config/dominion_ignore_policy.json`, then run a dry-run stale-index repair report for orphan active chunks before soft-deleting historical temp paths.

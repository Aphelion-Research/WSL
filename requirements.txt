# Minimal runtime + tooling dependencies for Dominion Python components.
# Keep this list tight: only third-party imports used in-repo.

duckdb>=1.0.0
polars>=0.20.0
PyYAML>=6.0
requests>=2.31.0
numpy>=1.26.0

# Embedding providers.
voyageai>=0.2.0
openai>=1.0.0

# AST chunking and persistent semantic search.
tree-sitter>=0.21.0
tree-sitter-python>=0.21.0
tree-sitter-cpp>=0.22.0
tree-sitter-typescript>=0.21.0
tree-sitter-javascript>=0.21.0
tree-sitter-rust>=0.21.0
tree-sitter-go>=0.21.0
hnswlib>=0.8.0

# Test runner for `python -m pytest -q`.
pytest>=8.0.0
